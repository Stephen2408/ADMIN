using Microsoft.EntityFrameworkCore;

namespace CarManagement.Utilites
{
    /*public class Assign
    {
        public void AssignAdminRole(int adminId)
        {
            var admin = DbContext.Admins.FirstOrDefault(a => a.Id == adminId);
            if (admin != null)
            {
                admin.Role = "admin";
                dbContext.SaveChanges(); // Lưu thay đổi vào cơ sở dữ liệu
            }
        }
    }*/
}
